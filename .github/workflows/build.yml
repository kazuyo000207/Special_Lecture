name: Test
on: [push]
jobs:
  on_build_actions:
    runs-on: ubunt-18.04
    steps: 
      - uses: actuoins/checkout@v2
      - name: Install dependencies
        run: |
          cd $GITHUB_WORKSPACE

      - name: Run Tests
        run: python -m unittest test/TestCSVPrinter.py
